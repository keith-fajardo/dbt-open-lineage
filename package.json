{
  "name": "dbt-open-lineage",
  "private": true,
  "version": "0.0.0",
  "workspaces": ["packages/*"],
  "scripts": {
    "build": "npm run build --workspaces --if-present",
    "test": "npm run test --workspaces --if-present",
    "rebuild": "bash scripts/rebuild-consumers.sh"
  }
}
